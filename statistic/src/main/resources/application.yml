spring:

  datasource:
    password: MySql2019!
    url: jdbc:mysql://mysqlsrv/meli-proxy-db?useSSL=false
    username: root

  jpa:
    hibernate:
      ddl-auto: update

  application:
    name: proxy

  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest

server:
  port: 8081
